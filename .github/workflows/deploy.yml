name: Deploy to GitHub Pages

# Manual dispatch and the default branch only, so nothing publishes by accident
# from a working branch.
on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm

      # --omit=dev skips Playwright, which is only needed for the local capture
      # harness and would otherwise pull a browser down on every run.
      - name: Install
        run: npm ci --omit=dev

      # Generates the character, the world and the vendored three.js into web/.
      - name: Build assets
        run: npm run build:assets

      - uses: actions/configure-pages@v5
        with:
          enablement: true

      - uses: actions/upload-pages-artifact@v3
        with:
          path: web

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
